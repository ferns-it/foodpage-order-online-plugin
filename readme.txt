# Order Online Plugin Setup

## Steps to Follow After Cloning the Plugin

1. Open your terminal and navigate to the project directory where you've cloned the plugin.

3. create new branch from the main [by project name]

4. Run the following command to install the necessary dependencies:

    - npm install axios react-icons react-hot-toast react-loading-skeleton crypto-js react-spinners jsonwebtoken react-router-dom react-otp-input @stripe/stripe-js @stripe/react-stripe-js react-stripe-js react-lottie

    - install the package outside outside the project.

5. There is an env file copy it into your project 

6. After the installation completes, you can start the development server using:
    - npm run dev

7. The project should now be set up and ready to use! [test credentials]

## Note:
- Make sure you have Node.js and npm installed on your system.
- If any other environment-specific setup is required, refer to the documentation or project details.
